import { Routes } from '@angular/router';
import { DataEntryExampleComponent } from './data-entry-example.component';

export const DATAENTRYEXAMPLE_ROUTES: Routes = [
  { path: '', component: DataEntryExampleComponent },
];
