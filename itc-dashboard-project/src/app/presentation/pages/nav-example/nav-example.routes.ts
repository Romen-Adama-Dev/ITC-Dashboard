import { Routes } from '@angular/router';
import { NavExamplesComponent } from './nav-example.component';

export const NAV_EXAMPLES_ROUTES: Routes = [
  { path: '', component: NavExamplesComponent },
];
